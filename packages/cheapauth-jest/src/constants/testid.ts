export enum TestId {
  root = 'cheapauth--root',
  title = 'cheapauth--title',
  form = 'cheapauth--form',
  input = 'cheapauth--input',
  errorMessage = 'cheapauth--error-message',
  submit = 'cheapauth--submit',
}

export default TestId;
